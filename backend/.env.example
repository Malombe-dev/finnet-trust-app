PORT=5000
# If MONGO_URI is left empty, the API automatically falls back to an
# in-memory data store seeded with sample users & posts, so the project
# runs out of the box with zero external setup.
MONGO_URI=
CLIENT_ORIGIN=http://localhost:5173
